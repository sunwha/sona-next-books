import styles from "./styles/loading.module.css";

export default function NotFound() {
  return (
    <section className={styles.section}>
      <h1 className={styles.loading}>Not Found</h1>
    </section>
  );
}
